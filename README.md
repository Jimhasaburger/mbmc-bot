# MBMC Commands

Command reference for the in-game `+` command system.

## Commands

- `+ping`  
  Replies with the bots ping: `Pong! <ping>ms`

- `+funfact`  
  Replies with a random fun fact.

- `+random <min> <max>`  
  Replies with a random integer between `min` and `max` (inclusive).  
  Example: `+random 1 100`

- `+onlineplayers`  
  Replies with: `N players are online.`

- `+help`  
  Replies with this GitHub link.

## Info

- Prefix is `+`
- Commands have a 5-second cooldown
